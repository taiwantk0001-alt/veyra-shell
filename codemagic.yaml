workflows:
  ios-shell:
    name: Build iOS WebView Shell (base.ipa)
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      xcode: latest
    scripts:
      - name: Build unsigned ShellApp
        script: |
          set -e
          chmod +x tools/build-ios-shell.sh
          bash tools/build-ios-shell.sh
          ls -lh ios-shell/base.ipa
    artifacts:
      - ios-shell/base.ipa
